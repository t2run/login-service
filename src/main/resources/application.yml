spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/user_auth
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
server:
  port: 8899
  

